{
  "$schema": "https://www.schemastore.org/package.json",
  "name": "be-kanban-board",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Simple Kanban board task API for backend handoff demonstrations.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "express": "^5.1.0",
    "swagger-ui-express": "^5.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
